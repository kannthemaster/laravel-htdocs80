@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">VisitMedicine {{ $visitmedicine->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/backend/visit-medicine') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/backend/visit-medicine/' . $visitmedicine->id . '/edit') }}" title="Edit VisitMedicine"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('backend/visitmedicine' . '/' . $visitmedicine->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete VisitMedicine" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $visitmedicine->id }}</td>
                                    </tr>
                                    <tr><th> Visit Id </th><td> {{ $visitmedicine->visit_id }} </td></tr><tr><th> Medicine </th><td> {{ $visitmedicine->medicine }} </td></tr><tr><th> Method </th><td> {{ $visitmedicine->method }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
