@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Diagnosi {{ $diagnosi->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/backend/diagnosis') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/backend/diagnosis/' . $diagnosi->id . '/edit') }}" title="Edit Diagnosi"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('backend/diagnosis' . '/' . $diagnosi->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete Diagnosi" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $diagnosi->id }}</td>
                                    </tr>
                                    <tr><th> Visit Id </th><td> {{ $diagnosi->visit_id }} </td></tr><tr><th> Disease </th><td> {{ $diagnosi->disease }} </td></tr><tr><th> Other Disease </th><td> {{ $diagnosi->other_disease }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
